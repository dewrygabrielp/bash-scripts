# Bash Scripts

A collection of Bash utilities for Linux.

Each tool is self-contained inside its own directory and includes its own documentation and dependencies.

## Available Scripts

| Script | Description |
|---------|-------------|
| forensic_pdf | Cleans PDF metadata, rebuilds PDF structure, and generates a forensic report before and after processing. |

More scripts will be added over time.
